<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_escrow_car_brand_tree" model="ir.ui.view">
        <field name="name">escrow.car.brand.tree</field>
        <field name="model">escrow.car.brand</field>
        <field name="arch" type="xml">
            <tree js_class="brand_buttons">
                <field name="name"/>
                <field name="brand_id"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <record id="view_escrow_car_brand_form" model="ir.ui.view">
        <field name="name">escrow.car.brand.form</field>
        <field name="model">escrow.car.brand</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button type="object" class="btn btn-primary" name="action_get_models" string="Sync Models" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                    </div>
                    <field name="logo" widget="image" class="oe_avatar" options="{'preview_image': 'logo', 'size': [90, 90]}"/>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Brand Name"/>
                        </h1>
                    </div>
                    <group>
                        <field name="active"/>
                        <field name="brand_id"/>
                    </group>
                    <notebook>
                        <page string="Models" name="models">
                            <field name="model_ids">
                                <tree editable="bottom">
                                    <field name="name"/>
                                    <field name="active"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_escrow_vehicle_brand" model="ir.actions.act_window">
        <field name="name">Vehicle Brands</field>
        <field name="res_model">escrow.car.brand</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first vehicle brand
            </p>
        </field>
    </record>

</odoo>
